#lang setup/infotab
(define name "SVN Properties")
(define release-notes
  (list '(ul
          (li "Initial release"))))
(define repositories
  (list "4.x"))
(define blurb
  (list "A small FFI for Subversion WC properties"))
(define scribblings '(("svn-prop.scrbl" ())))
(define primary-file "main.ss")
(define categories '(io))